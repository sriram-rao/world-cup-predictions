<main class="mx-auto w-full max-w-5xl px-4 py-10 sm:px-6 sm:py-14 lg:px-8 lg:py-20">
  <% title_extra = link_to "FotMob ↗", "https://www.fotmob.com/leagues/77/fixtures/world-cup?group=by-date",
    target: "_blank",
    rel: "noopener noreferrer",
    class: "shrink-0 rounded-lg border border-slate-200 bg-white px-2.5 py-1.5 text-sm font-black text-slate-500 shadow-sm transition hover:bg-slate-50 hover:text-slate-950",
    aria: { label: "Open World Cup fixtures on FotMob in a new tab" } %>

  <% actions = capture do %>
    <div class="grid gap-2">
      <div class="flex items-center justify-between gap-2">
      <%= form_with url: root_path, method: :get, class: "relative min-w-0" do |form| %>
        <span class="inline-flex max-w-full cursor-pointer items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-bold text-slate-700 shadow-sm transition hover:bg-slate-50 hover:text-slate-950 sm:px-4 sm:text-base">
          <span aria-hidden="true">📅</span>
          <span class="truncate sm:hidden"><%= @fixture_date.strftime("%b %-d") %> (US)</span>
          <span class="hidden sm:inline">Matchday: <%= @fixture_date.strftime("%A, %B %-d, %Y") %> (US)</span>
        </span>
        <%= form.date_field :date,
          value: @fixture_date.iso8601,
          onchange: "this.form.requestSubmit()",
          onclick: "this.showPicker && this.showPicker()",
          aria: { label: "Choose fixture date" },
          class: "absolute inset-0 z-10 h-full w-full cursor-pointer opacity-[0.01]" %>
      <% end %>

      <nav class="grid w-36 shrink-0 grid-cols-[2.5rem_4rem_2.5rem] overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm sm:w-64 sm:grid-cols-3" aria-label="Fixture date navigation">
        <%= link_to "←", root_path(date: @previous_date.iso8601), class: "border-r border-slate-200 px-2 py-3 text-center text-sm font-semibold text-slate-700 transition hover:bg-slate-50 sm:px-4" %>
        <%= link_to "Today", root_path, class: "border-r border-slate-200 px-2 py-3 text-center text-sm font-semibold text-slate-700 transition hover:bg-slate-50 sm:px-4" %>
        <%= link_to "→", root_path(date: @next_date.iso8601), class: "px-2 py-3 text-center text-sm font-semibold text-slate-700 transition hover:bg-slate-50 sm:px-4" %>
      </nav>
      </div>

      <% if admin_mode? %>
        <%= button_to "Update results", import_results_path,
          method: :post,
          params: { date: @fixture_date.iso8601 },
          class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-black text-slate-700 shadow-sm transition hover:bg-slate-50" %>
      <% end %>
    </div>
  <% end %>

  <%= render "shared/page_header", title: "Fixtures", title_extra: title_extra, actions: actions %>

  <% if flash[:notice].present? %>
    <div class="mb-6 rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700"><%= flash[:notice] %></div>
  <% end %>

  <% if flash[:alert].present? %>
    <div class="mb-6 rounded-2xl border border-red-200 bg-red-50 px-4 py-3 text-sm font-semibold text-red-700"><%= flash[:alert] %></div>
  <% end %>

  <% if @fixtures.any? %>
    <section class="grid gap-4 lg:grid-cols-2" aria-label="Fixtures">
      <%= render partial: "fixture", collection: @fixtures, locals: { predictions_by_fixture_id: @predictions_by_fixture_id } %>
    </section>

    <% unless authenticated? %>
      <div class="mt-6 flex justify-center">
        <%= link_to "Sign in to predict", new_session_path, class: "inline-block rounded-xl bg-slate-950 px-4 py-3 text-sm font-black text-white transition hover:bg-slate-800" %>
      </div>
    <% end %>
  <% else %>
    <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm shadow-slate-200/70 sm:rounded-3xl sm:p-8">
      <h2 class="text-2xl font-black text-slate-950">No fixtures today</h2>
      <p class="mt-2 text-slate-600">Check back when matchday starts.</p>
    </section>
  <% end %>
</main>
